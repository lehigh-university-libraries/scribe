// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: transcription_job_leases.sql

package db

import (
	"context"
	"database/sql"
	"time"
)

const claimLeasedTranscriptionJobByIDManual = `-- name: ClaimLeasedTranscriptionJobByIDManual :one
SELECT
  id,
  workspace_id,
  item_image_id,
  context_id,
  context_scope_id,
  context_snapshot,
  input_revision,
  status,
  active_item_image_id,
  total_segments,
  completed_segments,
  failed_segments,
  attempt_count,
  max_attempts,
  retry_after,
  lease_until,
  locked_by,
  current_annotation_id,
  current_annotation_json,
  last_result_annotation_json,
  error_message,
  created_at,
  updated_at
FROM transcription_jobs
WHERE id = ?
  AND (
    (
      status = 'pending'
      AND (retry_after IS NULL OR retry_after <= NOW())
      AND attempt_count < max_attempts
    ) OR (
      status = 'running'
      AND lease_until IS NOT NULL
      AND lease_until < NOW()
    )
  )
LIMIT 1
FOR UPDATE SKIP LOCKED
`

func (q *Queries) ClaimLeasedTranscriptionJobByIDManual(ctx context.Context, id uint64) (TranscriptionJob, error) {
	row := q.db.QueryRowContext(ctx, claimLeasedTranscriptionJobByIDManual, id)
	var i TranscriptionJob
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.ItemImageID,
		&i.ContextID,
		&i.ContextScopeID,
		&i.ContextSnapshot,
		&i.InputRevision,
		&i.Status,
		&i.ActiveItemImageID,
		&i.TotalSegments,
		&i.CompletedSegments,
		&i.FailedSegments,
		&i.AttemptCount,
		&i.MaxAttempts,
		&i.RetryAfter,
		&i.LeaseUntil,
		&i.LockedBy,
		&i.CurrentAnnotationID,
		&i.CurrentAnnotationJson,
		&i.LastResultAnnotationJson,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const claimNextLeasedTranscriptionJobManual = `-- name: ClaimNextLeasedTranscriptionJobManual :one
SELECT
  id,
  workspace_id,
  item_image_id,
  context_id,
  context_scope_id,
  context_snapshot,
  input_revision,
  status,
  active_item_image_id,
  total_segments,
  completed_segments,
  failed_segments,
  attempt_count,
  max_attempts,
  retry_after,
  lease_until,
  locked_by,
  current_annotation_id,
  current_annotation_json,
  last_result_annotation_json,
  error_message,
  created_at,
  updated_at
FROM transcription_jobs
WHERE (
    status = 'pending'
    AND (retry_after IS NULL OR retry_after <= NOW())
    AND attempt_count < max_attempts
  ) OR (
    status = 'running'
    AND lease_until IS NOT NULL
    AND lease_until < NOW()
  )
ORDER BY created_at ASC
LIMIT 1
FOR UPDATE SKIP LOCKED
`

func (q *Queries) ClaimNextLeasedTranscriptionJobManual(ctx context.Context) (TranscriptionJob, error) {
	row := q.db.QueryRowContext(ctx, claimNextLeasedTranscriptionJobManual)
	var i TranscriptionJob
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.ItemImageID,
		&i.ContextID,
		&i.ContextScopeID,
		&i.ContextSnapshot,
		&i.InputRevision,
		&i.Status,
		&i.ActiveItemImageID,
		&i.TotalSegments,
		&i.CompletedSegments,
		&i.FailedSegments,
		&i.AttemptCount,
		&i.MaxAttempts,
		&i.RetryAfter,
		&i.LeaseUntil,
		&i.LockedBy,
		&i.CurrentAnnotationID,
		&i.CurrentAnnotationJson,
		&i.LastResultAnnotationJson,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const claimNextLeasedTranscriptionJobOlderThanManual = `-- name: ClaimNextLeasedTranscriptionJobOlderThanManual :one
SELECT
  id,
  workspace_id,
  item_image_id,
  context_id,
  context_scope_id,
  context_snapshot,
  input_revision,
  status,
  active_item_image_id,
  total_segments,
  completed_segments,
  failed_segments,
  attempt_count,
  max_attempts,
  retry_after,
  lease_until,
  locked_by,
  current_annotation_id,
  current_annotation_json,
  last_result_annotation_json,
  error_message,
  created_at,
  updated_at
FROM transcription_jobs
WHERE created_at < ?
  AND (
    (
      status = 'pending'
      AND (retry_after IS NULL OR retry_after <= NOW())
      AND attempt_count < max_attempts
    ) OR (
      status = 'running'
      AND lease_until IS NOT NULL
      AND lease_until < NOW()
    )
  )
ORDER BY created_at ASC
LIMIT 1
FOR UPDATE SKIP LOCKED
`

func (q *Queries) ClaimNextLeasedTranscriptionJobOlderThanManual(ctx context.Context, cutoff time.Time) (TranscriptionJob, error) {
	row := q.db.QueryRowContext(ctx, claimNextLeasedTranscriptionJobOlderThanManual, cutoff)
	var i TranscriptionJob
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.ItemImageID,
		&i.ContextID,
		&i.ContextScopeID,
		&i.ContextSnapshot,
		&i.InputRevision,
		&i.Status,
		&i.ActiveItemImageID,
		&i.TotalSegments,
		&i.CompletedSegments,
		&i.FailedSegments,
		&i.AttemptCount,
		&i.MaxAttempts,
		&i.RetryAfter,
		&i.LeaseUntil,
		&i.LockedBy,
		&i.CurrentAnnotationID,
		&i.CurrentAnnotationJson,
		&i.LastResultAnnotationJson,
		&i.ErrorMessage,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const completeTranscriptionJobLeasedManual = `-- name: CompleteTranscriptionJobLeasedManual :execresult
UPDATE transcription_jobs
SET
  status = 'completed',
  current_annotation_id = NULL,
  current_annotation_json = NULL,
  last_result_annotation_json = NULL,
  lease_until = NULL,
  locked_by = NULL,
  updated_at = NOW()
WHERE id = ?
  AND attempt_count = ?
  AND input_revision = ?
  AND COALESCE(locked_by, '') = ?
  AND status = 'running'
  AND lease_until IS NOT NULL
  AND lease_until > NOW()
`

type CompleteTranscriptionJobLeasedManualParams struct {
	ID            uint64         `json:"id"`
	AttemptNumber int32          `json:"attempt_number"`
	InputRevision uint64         `json:"input_revision"`
	LeaseToken    sql.NullString `json:"lease_token"`
}

func (q *Queries) CompleteTranscriptionJobLeasedManual(ctx context.Context, arg CompleteTranscriptionJobLeasedManualParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, completeTranscriptionJobLeasedManual,
		arg.ID,
		arg.AttemptNumber,
		arg.InputRevision,
		arg.LeaseToken,
	)
}

const deferTranscriptionJobLeaseManual = `-- name: DeferTranscriptionJobLeaseManual :execresult
UPDATE transcription_jobs
SET
  status = 'pending',
  retry_after = ?,
  error_message = ?,
  current_annotation_id = NULL,
  current_annotation_json = NULL,
  last_result_annotation_json = NULL,
  lease_until = NULL,
  locked_by = NULL,
  updated_at = NOW()
WHERE id = ?
  AND attempt_count = ?
  AND input_revision = ?
  AND COALESCE(locked_by, '') = ?
  AND status = 'running'
  AND lease_until IS NOT NULL
  AND lease_until > NOW()
`

type DeferTranscriptionJobLeaseManualParams struct {
	RetryAfter    sql.NullTime   `json:"retry_after"`
	ErrorMessage  sql.NullString `json:"error_message"`
	ID            uint64         `json:"id"`
	AttemptNumber int32          `json:"attempt_number"`
	InputRevision uint64         `json:"input_revision"`
	LeaseToken    sql.NullString `json:"lease_token"`
}

func (q *Queries) DeferTranscriptionJobLeaseManual(ctx context.Context, arg DeferTranscriptionJobLeaseManualParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, deferTranscriptionJobLeaseManual,
		arg.RetryAfter,
		arg.ErrorMessage,
		arg.ID,
		arg.AttemptNumber,
		arg.InputRevision,
		arg.LeaseToken,
	)
}

const extendTranscriptionJobLeaseManual = `-- name: ExtendTranscriptionJobLeaseManual :execresult
UPDATE transcription_jobs
SET
  lease_until = ?,
  updated_at = NOW()
WHERE id = ?
  AND attempt_count = ?
  AND input_revision = ?
  AND COALESCE(locked_by, '') = ?
  AND status = 'running'
  AND lease_until IS NOT NULL
  AND lease_until > NOW()
`

type ExtendTranscriptionJobLeaseManualParams struct {
	LeaseUntil    sql.NullTime   `json:"lease_until"`
	ID            uint64         `json:"id"`
	AttemptNumber int32          `json:"attempt_number"`
	InputRevision uint64         `json:"input_revision"`
	LeaseToken    sql.NullString `json:"lease_token"`
}

func (q *Queries) ExtendTranscriptionJobLeaseManual(ctx context.Context, arg ExtendTranscriptionJobLeaseManualParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, extendTranscriptionJobLeaseManual,
		arg.LeaseUntil,
		arg.ID,
		arg.AttemptNumber,
		arg.InputRevision,
		arg.LeaseToken,
	)
}

const failExpiredTranscriptionJobManual = `-- name: FailExpiredTranscriptionJobManual :execresult
UPDATE transcription_jobs
SET status = 'failed',
    retry_after = NULL,
    error_message = 'worker lease expired after maximum attempts',
    current_annotation_id = NULL,
    current_annotation_json = NULL,
    last_result_annotation_json = NULL,
    lease_until = NULL,
    locked_by = NULL,
    updated_at = NOW()
WHERE id = ?
  AND status = 'running'
  AND lease_until IS NOT NULL
  AND lease_until < NOW()
  AND attempt_count = ?
  AND input_revision = ?
  AND COALESCE(locked_by, '') = ?
  AND attempt_count >= max_attempts
`

type FailExpiredTranscriptionJobManualParams struct {
	ID            uint64         `json:"id"`
	AttemptNumber int32          `json:"attempt_number"`
	InputRevision uint64         `json:"input_revision"`
	LeaseToken    sql.NullString `json:"lease_token"`
}

func (q *Queries) FailExpiredTranscriptionJobManual(ctx context.Context, arg FailExpiredTranscriptionJobManualParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, failExpiredTranscriptionJobManual,
		arg.ID,
		arg.AttemptNumber,
		arg.InputRevision,
		arg.LeaseToken,
	)
}

const lockActiveTranscriptionJobLeaseManual = `-- name: LockActiveTranscriptionJobLeaseManual :one
SELECT id
FROM transcription_jobs
WHERE id = ?
  AND attempt_count = ?
  AND input_revision = ?
  AND COALESCE(locked_by, '') = ?
  AND status = 'running'
  AND lease_until IS NOT NULL
  AND lease_until > NOW()
FOR UPDATE
`

type LockActiveTranscriptionJobLeaseManualParams struct {
	ID            uint64         `json:"id"`
	AttemptNumber int32          `json:"attempt_number"`
	InputRevision uint64         `json:"input_revision"`
	LeaseToken    sql.NullString `json:"lease_token"`
}

func (q *Queries) LockActiveTranscriptionJobLeaseManual(ctx context.Context, arg LockActiveTranscriptionJobLeaseManualParams) (uint64, error) {
	row := q.db.QueryRowContext(ctx, lockActiveTranscriptionJobLeaseManual,
		arg.ID,
		arg.AttemptNumber,
		arg.InputRevision,
		arg.LeaseToken,
	)
	var id uint64
	err := row.Scan(&id)
	return id, err
}

const markTranscriptionJobLeasedManual = `-- name: MarkTranscriptionJobLeasedManual :execresult
UPDATE transcription_jobs
SET
  status = 'running',
  attempt_count = attempt_count + 1,
  total_segments = 0,
  completed_segments = 0,
  failed_segments = 0,
  current_annotation_id = NULL,
  current_annotation_json = NULL,
  last_result_annotation_json = NULL,
  lease_until = ?,
  locked_by = ?,
  retry_after = NULL,
  error_message = NULL,
  updated_at = NOW()
WHERE id = ?
  AND attempt_count = ?
  AND input_revision = ?
  AND (locked_by <=> ?)
  AND (
    (
      status = 'pending'
      AND (retry_after IS NULL OR retry_after <= NOW())
      AND attempt_count < max_attempts
    ) OR (
      status = 'running'
      AND lease_until IS NOT NULL
      AND lease_until < NOW()
      AND attempt_count < max_attempts
    )
  )
`

type MarkTranscriptionJobLeasedManualParams struct {
	LeaseUntil           sql.NullTime   `json:"lease_until"`
	LeaseToken           sql.NullString `json:"lease_token"`
	ID                   uint64         `json:"id"`
	PreviousAttemptCount int32          `json:"previous_attempt_count"`
	InputRevision        uint64         `json:"input_revision"`
	PreviousLeaseToken   sql.NullString `json:"previous_lease_token"`
}

func (q *Queries) MarkTranscriptionJobLeasedManual(ctx context.Context, arg MarkTranscriptionJobLeasedManualParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, markTranscriptionJobLeasedManual,
		arg.LeaseUntil,
		arg.LeaseToken,
		arg.ID,
		arg.PreviousAttemptCount,
		arg.InputRevision,
		arg.PreviousLeaseToken,
	)
}

const retryOrFailTranscriptionJobManual = `-- name: RetryOrFailTranscriptionJobManual :execresult
UPDATE transcription_jobs
SET
  status = CASE WHEN attempt_count < max_attempts THEN 'pending' ELSE 'failed' END,
  retry_after = CASE WHEN attempt_count < max_attempts THEN DATE_ADD(NOW(), INTERVAL LEAST(300, POW(2, attempt_count) * 10) SECOND) ELSE NULL END,
  error_message = ?,
  current_annotation_id = NULL,
  current_annotation_json = NULL,
  lease_until = NULL,
  locked_by = NULL,
  updated_at = NOW()
WHERE id = ?
  AND attempt_count = ?
  AND input_revision = ?
  AND COALESCE(locked_by, '') = ?
  AND status = 'running'
  AND lease_until IS NOT NULL
  AND lease_until > NOW()
`

type RetryOrFailTranscriptionJobManualParams struct {
	ErrorMessage  sql.NullString `json:"error_message"`
	ID            uint64         `json:"id"`
	AttemptNumber int32          `json:"attempt_number"`
	InputRevision uint64         `json:"input_revision"`
	LeaseToken    sql.NullString `json:"lease_token"`
}

func (q *Queries) RetryOrFailTranscriptionJobManual(ctx context.Context, arg RetryOrFailTranscriptionJobManualParams) (sql.Result, error) {
	return q.db.ExecContext(ctx, retryOrFailTranscriptionJobManual,
		arg.ErrorMessage,
		arg.ID,
		arg.AttemptNumber,
		arg.InputRevision,
		arg.LeaseToken,
	)
}
